# Auth App

Purpose
Authentication and session setup for external broker APIs used by the engine.

Key Modules
- `app.py`
- `logic.py`
- `settings.py`

Configuration
Settings defaults live in `settings.py` and can be overridden by root config (e.g. `configs/bot.yaml`) or env vars.
