# Sentinel App

Purpose
Alerting and notification logic (regime changes, heartbeats, signals).

Key Modules
- `app.py`
- `core.py`
- `events.py`
- `settings.py`

Configuration
Settings defaults live in `settings.py` and can be overridden by root config (e.g. `configs/bot.yaml`) or env vars.
