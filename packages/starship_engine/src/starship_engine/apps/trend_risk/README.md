# Trend Risk App

Purpose
Trend-risk scoring and secondary gate logic.

Key Modules
- `app.py`
- `logic.py`
- `settings.py`

Configuration
Settings defaults live in `settings.py` and can be overridden by root config (e.g. `configs/bot.yaml`) or env vars.
