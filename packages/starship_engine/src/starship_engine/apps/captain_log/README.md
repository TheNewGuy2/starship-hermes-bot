# Captain Log App

Purpose
State logging and lifecycle/audit output for the running engine.

Key Modules
- `app.py`
- `state_log.py`
- `state.py`
- `settings.py`

Configuration
Settings defaults live in `settings.py` and can be overridden by root config (e.g. `configs/bot.yaml`) or env vars.
