# Overnight Context App

Purpose
Computes overnight context priors used by the engine and gates.

Key Modules
- `app.py`
- `logic.py`
- `settings.py`

Configuration
Settings defaults live in `settings.py` and can be overridden by root config (e.g. `configs/bot.yaml`) or env vars.
