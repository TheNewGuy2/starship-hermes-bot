# Stream App

Purpose
Market data stream ingestion and symbol resolution.

Key Modules
- `app.py`
- `stream.py`
- `symbols.py`
- `settings.py`

Configuration
Settings defaults live in `settings.py` and can be overridden by root config (e.g. `configs/bot.yaml`) or env vars.
