# Directives App

Purpose
Directive and policy helpers used by the engine to interpret rules.

Key Modules
- `app.py`
- `policy.py`

Configuration
No dedicated `settings.py` yet; configuration is handled by the root config and app modules.
